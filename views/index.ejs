<h2>Ajouts récents</h2>

<div>
    <% books.forEach(book => { %>
    <a href="/books/<%= book.id %>">
        <img src="<%= book.coverImagePath %>" alt="" height="150" width="100">
    </a>
    <% }) %>
</div>